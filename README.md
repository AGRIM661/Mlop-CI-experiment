# Mlop-CI-experiment
MLOps-CI-Experiment is an MLOps project that uses Continuous Integration (CI) to automatically test and validate machine learning code whenever changes are pushed to GitHub. It helps detect errors early and ensures that the ML pipeline remains reliable and reproducible.
